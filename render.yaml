services:
  - type: web
    name: devduck-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PARCLE_API_KEY
        sync: false
